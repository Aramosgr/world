describe('User journey - logged out', () => {
  //TODO: the tests need to be changed to suit the differences between pick 5 and pick 6
  //process.env.REACT_APP_GAME_TYPE is undefined and seemingly can't be accessed here so we need a way to distinguish between the two
});
